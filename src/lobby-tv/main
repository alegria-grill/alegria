#!/usr/bin/env zsh

set -x
cd /apps/alegria && git pull || :

echo "=== Current directory: $PWD"
echo "=== Date: $(date)"
cd /home/pi/images
fbi -a -t 5 -noverbose  *.jpg *.png || {
  echo "!!! $?"
  sleep 5
}
