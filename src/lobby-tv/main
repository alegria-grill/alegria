#!/usr/bin/env zsh

set -x

echo "=== Current directory: $PWD"
echo "=== Date: $(date)"
cd $HOME
find images/ -type f -iname '*.jpg' -or -iname '*.png' > list.txt
fbi -a -blend 400 -t 5 -noverbose -u -l $PWD/list.txt || {
  echo "!!! Failed with exit status: $?"
}
